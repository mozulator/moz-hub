# Hub / global
NODE_ENV=development

# Budget Tracker (mounted at /p/budget-tracker)
# Use schema-per-app in Postgres:
# postgresql://postgres:postgres@localhost:5432/projects?schema=budget_tracker
BUDGET_TRACKER_DATABASE_URL=

# Required: session secret for Budget Tracker
BUDGET_TRACKER_SESSION_SECRET=

# Required for initial seed (create/update the single user)
BUDGET_TRACKER_ADMIN_USERNAME=admin
BUDGET_TRACKER_ADMIN_PASSWORD=change_me

# Optional: enables AI PDF import + compare summarization
OPENAI_API_KEY=


